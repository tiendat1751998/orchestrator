# Micro-Task 1.34: Create contracts/feedback/feedback.go

## Info
- **File**: `contracts/feedback/feedback.go`
- **Package**: `feedback`
- **Depends on**: 1.06
- **Time**: 10 min
- **Verify**: `go build ./contracts/feedback/...`

## Purpose
Declares performance assessment contracts (`Evaluator`, `EvalInput`, `EvalResult`, `Scorer`, `AgentScore`) enabling LLM output verification and agent task ranking.

## EXACT code to create

```go
// Package feedback defines contracts for quality evaluation and agent scoring.
// The feedback system enables continuous improvement of agent performance.
package feedback

import "context"

// Evaluator assesses the quality of agent outputs.
//
// Evaluation methods:
//   - Heuristic: "go build" passes? "go test" passes? (fast, reliable)
//   - AI-as-judge: ask another AI to score the output (flexible, but costly)
type Evaluator interface {
	// Evaluate scores an agent's output.
	//
	// Returns a score (0-1) and feedback text.
	// Score interpretation:
	//   0.0 - 0.3: Poor quality
	//   0.3 - 0.6: Acceptable
	//   0.6 - 0.8: Good
	//   0.8 - 1.0: Excellent
	Evaluate(ctx context.Context, input EvalInput) (*EvalResult, error)
}

// EvalInput is the data to evaluate.
type EvalInput struct {
	// TaskType is the type of task that was executed.
	TaskType string `json:"task_type"`

	// AgentName identifies which agent produced the output.
	AgentName string `json:"agent_name"`

	// Output is the agent's text output to evaluate.
	Output string `json:"output"`

	// Expected is the expected output (if available).
	// Used for comparison-based evaluation.
	Expected string `json:"expected,omitempty"`
}

// EvalResult is the evaluation outcome.
type EvalResult struct {
	// Score is the quality score (0.0 to 1.0).
	Score float64 `json:"score"`

	// Feedback is a human-readable explanation of the score.
	Feedback string `json:"feedback"`

	// Pass indicates whether the output meets minimum quality standards.
	// Threshold is configured per evaluator (typically 0.6).
	Pass bool `json:"pass"`
}

// Scorer tracks agent performance over time.
//
// The scorer aggregates evaluation results and computes
// per-agent, per-task-type performance metrics.
//
// Used by the planner to select the best agent for a task.
type Scorer interface {
	// RecordResult records a task execution result for scoring.
	//
	// Parameters:
	//   - agentName: which agent executed the task
	//   - taskType: the type of task (e.g., "code_generation")
	//   - success: whether the task succeeded
	//   - duration: execution time in seconds
	RecordResult(agentName, taskType string, success bool, duration float64)

	// GetScore returns the performance score for an agent on a task type.
	// Returns 0.0 if no data is available.
	GetScore(agentName, taskType string) float64

	// GetRanking returns agents ranked by performance for a task type.
	// Best performers first.
	GetRanking(taskType string) []AgentScore
}

// AgentScore represents an agent's performance metrics.
type AgentScore struct {
	// AgentName identifies the agent.
	AgentName string `json:"agent_name"`

	// Score is the overall performance score (0-1).
	Score float64 `json:"score"`

	// TaskCount is the total number of tasks executed.
	TaskCount int `json:"task_count"`

	// SuccessRate is the percentage of successful tasks (0-100).
	SuccessRate float64 `json:"success_rate"`
}
```

## Rules
1. **Normalized Scoring**: All scores generated by `Evaluator` must reside strictly in the range `0.0` to `1.0`. Do not use `0` to `100` ranges.
2. **Scorer Concurrency**: `Scorer` implementations must protect internal aggregation maps using synchronization primitives (`sync.Mutex` or `sync.RWMutex`) to permit parallel agent task logging.
3. **Cold Start Selection**: If `GetScore` returns `0.0` due to a lack of past metrics, selector routines must proceed using default agent priority indexes.

## ⚠️ Pitfalls

### Pitfall 1: Not locking scorer maps in multi-threaded executions
If multiple agent workers call `RecordResult()` concurrently after finishing tasks, writing directly to maps inside `Scorer` without acquiring locks triggers concurrent map write panics, crashing the runtime execution process.

### Pitfall 2: High token costs from excessive AI-as-Judge evaluations
Relying entirely on AI-as-Judge models to evaluate minor tasks adds huge token latencies and high operating costs. Prefer fast, deterministic heuristic validation checks (like checking compiler output or running linter checks) and fallback to AI judges only when necessary.

## Verify
```bash
go build ./contracts/feedback/...
```

## Checklist
- [ ] File `contracts/feedback/feedback.go` exists
- [ ] Package: `feedback`
- [ ] `Evaluator` interface declares `Evaluate` method returning `(*EvalResult, error)`
- [ ] `EvalInput` and `EvalResult` structs are defined
- [ ] `Scorer` interface declares RecordResult, GetScore, and GetRanking methods
- [ ] `AgentScore` contains AgentName, Score, TaskCount, and SuccessRate fields
- [ ] `go build ./contracts/feedback/...` passes
